import 'package:flutter/material.dart';

class BurguerMasterTextField extends StatelessWidget {

  const BurguerMasterTextField({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}