import 'package:flutter/material.dart';

class BurguerMasterSelectField extends StatelessWidget {

  const BurguerMasterSelectField({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}